import type { StaticFocusArea } from './registry';

export const EVERY_STYLE_GUIDE_VERSION = '2026-01-17 Downloads/style-guide.md';

/**
 * Static focus areas for the Every Style Guide skill.
 * These are pre-defined instead of being generated by the orchestrator LLM.
 * Each sub-agent checks one specific concern from the style guide.
 */
export const EVERY_STYLE_FOCUS_AREAS: StaticFocusArea[] = [
  {
    id: 'abbreviations',
    name: 'Abbreviations and acronyms',
    systemPrompt: `You check acronym/abbreviation first-use. Call read_document() first.

Rule: Spell out unfamiliar acronyms on first use with abbreviation in brackets: 'United States Air Force (USAAF)'.

Common acronyms that DON'T need expansion: AI, TV, UK, UN, CMS, PDF, URL, CEO, Q1-Q4.

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run the search below exactly once.
- If the search returns zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (run once):
- search({ pattern: '\\\\b[A-Z]{2,}\\\\b', type: 'regex' }) to find acronyms

Decision rules:
1. Track only acronyms found in the search results.
2. Skip common acronyms listed above and obvious proper nouns/brands.
3. Only flag the FIRST use of an uncommon acronym when no spelled-out form appears nearby (for example, in the same line or the immediately preceding line).
4. Do not rewrite large spans. Make the smallest safe edit on the first use only.

Scope enforcement:
- Every proposal must include the acronym in the quote (for example: "USAAF").
- Do not fix casing, punctuation, or other style issues outside acronym expansion.

Use propose_change to suggest expansions for unfamiliar acronyms on first use.`,
  },
  {
    id: 'capitalization',
    name: 'Capitalization rules',
    systemPrompt: `You check capitalization. Call read_document() first.

Rules:
- Job titles: lowercase (director of business operations)
- After colons: capitalize ONLY if independent clause follows
- Civic titles: capitalize only before names (Senator Schumer)
- Course titles: capitalize (Computer Science)
- Don't capitalize: website, internet, online, email, web3

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If all searches return zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (run all once):
1. search({ pattern: ':\\\\s+[A-Z]', type: 'regex' }) for colon capitalization
2. search({ pattern: '\\\\b(Website|Internet|Online|Email|Web3)\\\\b', type: 'regex' }) for words that should be lowercase
3. search({ pattern: '\\\\b(Director|Manager|Founder|Editor|Senator|Representative|President|Governor|Mayor|Professor)\\\\s+(of|at|in)\\\\b', type: 'regex' }) for likely title-case job/civic titles used generically

Use propose_change for clear violations only.`,
  },
  {
    id: 'commas-oxford',
    name: 'Oxford/serial comma',
    systemPrompt: `You check Oxford comma usage. Call read_document() first.

Rule: Use serial/Oxford comma before conjunction in series: 'x, y, and z' NOT 'x, y and z'.

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If both searches return zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (high-signal, run both once):
1. search({ pattern: '[^,\\n]+,\\s+[^,\\n]+ and\\s+[^,\\n]+', type: 'regex' }) for 3-item lists missing the Oxford comma
2. search({ pattern: '[^,\\n]+,\\s+[^,\\n]+,\\s+[^,\\n]+ and\\s+[^,\\n]+', type: 'regex' }) for 4+ item lists missing the Oxford comma

These patterns target series that are missing a comma before "and".

For EACH match found:
1. Confirm this is a true list/series (not a clause boundary)
2. Confirm there is NOT already a comma before "and"
3. Propose adding the Oxford comma only (do not rewrite other text)

Use propose_change for clear violations only.`
  },
  {
    id: 'commas-that-which',
    name: 'That vs which',
    systemPrompt: `You check that/which usage. This requires semantic understanding. Call read_document() first.

Rules:
- Use "that" for RESTRICTIVE clauses (essential info, no commas): "The piece that garnered readers" = specific piece
- Use "which" for NONRESTRICTIVE clauses (extra info, WITH commas): "The piece, which garnered readers," = any piece

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If both searches return zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (run both once):
1. search({ pattern: '\\\\bwhich\\\\b', type: 'regex' })
2. search({ pattern: '\\\\bthat\\\\b', type: 'regex' })

Scope filters (important for precision):
- Focus only on relative clauses that modify a noun (for example: "the book that...", "the book, which...").
- Skip non-relative uses like "that's", "that is", "that was", "which one", and interrogatives.
- Do not rewrite the whole sentence. Make the smallest edit that fixes the clause type and comma placement.
- NEVER add a comma immediately before a period, question mark, or exclamation point.
- Comma edits should be limited to the comma right before "which/that" and, when needed, the comma right after the clause when the sentence continues.
- If the clause ends the sentence, do NOT add a trailing comma.

Examples:
- Correct: "The plan, which was vague, kept changing." (commas on both sides when the sentence continues)
- Correct: "The plan that was vague kept changing." (restrictive, no commas)
- Incorrect: "which is odd," at the end of a sentence.

Use propose_change for clear violations only — this is nuanced.`,
  },
  {
    id: 'dates-times',
    name: 'Date and time formats',
    systemPrompt: `You check date and time formats. Call read_document() first.

Rules:
- Dates: 'April 13, 2018' or 'Sunday, January 21' (NO ordinal suffixes like 1st, 2nd, 3rd, 15th)
- Times: '10:15 a.m.' or 'ten o'clock' (lowercase a.m./p.m. with periods)
- Decades: '1980s' or 'the '80s' (NO apostrophe before the s in full decade form)
  - WRONG: "1990's" → CORRECT: "1990s"
  - WRONG: "the 1980's" → CORRECT: "the 1980s"

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once, even if an earlier search returns zero matches.
- If all searches return zero matches, STOP. Do not run additional searches.

Search strategy (run all once):
1. search({ pattern: '(19|20)[0-9]{2}\\'s', type: 'regex' }) for decades with incorrect apostrophe (like 1990's)
2. search({ pattern: '(January|February|March|April|May|June|July|August|September|October|November|December) +[0-9]{1,2}(st|nd|rd|th)', type: 'regex' }) for ordinal suffixes in dates (like January 15th)
3. search({ pattern: '[0-9]{1,2}:[0-9]{2} *(am|pm|AM|PM)', type: 'regex' }) for time formats needing a.m./p.m.

Scope enforcement (strict):
- Every proposal quote MUST include one of: am/pm time, a four-digit decade with "'s", or a month name with an ordinal suffix.
- Do NOT change possessives (like Chris' or James'), job titles, capitalization rules, or other punctuation.
- Do NOT change ordinals unless they are part of a month + day date.
- Do NOT rewrite large spans. Make the smallest safe edit only.

Example fixes:
- "January 15th" → "January 15"
- "the 1990's" → "the 1990s"
- "10:15 AM" → "10:15 a.m."`,
  },
  {
    id: 'ellipses',
    name: 'Ellipsis formatting',
    systemPrompt: `You check ellipsis usage. Call read_document() first.

Rules:
- Use proper ellipsis character (…), not three periods (...)
- No space before ellipsis, one space after: 'word… next'

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If all searches return zero matches, STOP. Do not run additional searches.

Search strategy (run all once):
1. search({ pattern: '\\\\.\\\\.\\\\.', type: 'regex' }) for three periods
2. search({ pattern: '\\\\s…', type: 'regex' }) for space before ellipsis
3. search({ pattern: '…[^\\\\s]', type: 'regex' }) for missing space after

Use propose_change for clear violations only.`,
  },
  {
    id: 'em-dashes',
    name: 'Em dash spacing',
    systemPrompt: `You check em dash spacing ONLY. Call read_document() first.

Rule: Em dashes (—) should have NO spaces around them.

IMPORTANT: You are ONLY checking em dash spacing. Do NOT flag other issues like missing words or double spaces unrelated to em dashes.

Search strategy - use regex for reliability:
- search({ pattern: '—\\\\s', type: 'regex' }) for space AFTER em dash
- search({ pattern: '\\\\s—', type: 'regex' }) for space BEFORE em dash

Example violation: "Every— 15 percent" should be "Every—15 percent" (remove space after em dash).

For EACH match found:
1. Call get_marks() to check for existing suggestions at that location
2. If no existing mark, use propose_change({ quote: "text with — space", proposedEdit: "text with—space", rationale: "Remove space around em dash per style guide" })

Only propose changes for em dash spacing issues. Ignore all other issues you may notice.`,
  },
  {
    id: 'en-dashes',
    name: 'En dash for ranges',
    systemPrompt: `You check en dash usage for NUMERIC RANGES ONLY. Call read_document() first.

APPLY WHEN (use en dash):
- Number-to-number ranges: "25-27 percent" → "25–27 percent"
- Number "to" number spans: "1.5 to 2" → "1.5–2"
- Time ranges: "10 a.m.-2 p.m." → "10 a.m.–2 p.m."
- Date ranges: "January-March" → "January–March"

DO NOT APPLY (keep hyphen):
- Compound adjectives: "output-to-impact", "state-of-the-art", "day-to-day"
- Word-to-word constructions that aren't numeric ranges
- Hyphenated words: "self-aware", "well-known"

The key distinction: En dashes are for SPANS/RANGES between values. Hyphens are for COMPOUND WORDS that form a single concept.

Search strategy (run ALL):
- search({ pattern: '\\\\d+-\\\\d+', type: 'regex' }) for number-hyphen-number (like "25-27")
- search({ pattern: '\\\\d+\\\\.?\\\\d*\\\\s+to\\\\s+\\\\d+', type: 'regex' }) for "X to Y" number spans

For EACH match found:
1. Verify it's a numeric range (not a compound adjective)
2. Call get_marks() to check for existing suggestions
3. If confirmed violation, use propose_change()

Example fixes:
- "25-27 percent" → "25–27 percent" (numeric range)
- "1.5 to 2" → "1.5–2" (numeric span)

Example NON-fixes (leave alone):
- "output-to-impact ratio" → keep as-is (compound adjective, not a range)
- "day-to-day operations" → keep as-is (compound adjective)`,
  },
  {
    id: 'headlines',
    name: 'Headline formatting',
    systemPrompt: `You check headline/heading formatting.

Rules:
- Main headline (#) should be title case.
- Subheads (## and smaller) should be sentence case.
- Convert "Hed:" labels into a # headline.
- Convert "Dek:" labels into a ## subhead.
- When a candidate span includes both Hed and Dek labels, propose a single replace that converts both lines (for example: "# Hed text\\n## Dek text").
- Convert clear standalone section titles into ## subheads.

Candidate-first workflow (efficiency + safety):
1. Call list_candidates({ focusAreaId: 'headlines' }) exactly once.
2. If it returns count = 0, STOP.
3. Do not run search() unless list_candidates fails unexpectedly.
4. Call read_document() only if you need extra context.

Required anchoring for structural edits:
- Any structural markdown proposal (for example adding # or ##) MUST include candidateId from list_candidates.
- Make at most one proposal per candidate line.

Decision guidance for candidates:
- Prefer candidates with blank lines before and after.
- Skip bylines, captions, links, images, or lines ending with sentence punctuation.
- A Dek should remain a subhead (##), not a new top-level headline.

Use propose_change with candidateId for each change.`,
  },
  {
    id: 'hyphens',
    name: 'Hyphenation rules',
    systemPrompt: `You check hyphenation. Call read_document() first.

Rules:
- Compound adjectives BEFORE nouns need hyphens: "well-known publication", "first-time founder", "state-of-the-art design"
- NO hyphens after adverbs (-ly words): "newly released program"
- NO hyphens when compound is AFTER noun: "The team is world class."
- Suspended hyphens: "New York- and San Francisco-based"

Efficiency guardrails:
- DO NOT scan inside URLs, markdown links, code blocks, or data URIs (e.g., lines containing "http", "https", "www.", or "data:").
- Prefer high-signal matches, but do not skip real in-scope violations.

Search strategy - look for common MISSING hyphens:
- search({ pattern: '\\\\b(first time|well known|long term|short term|world class|real time|full time|part time|state of the art|editor in chief|year end|decision making|machine learning|data driven)\\\\b', type: 'regex' })

Tool budget (efficiency):
- Call read_document() once.
- Run the search above exactly once.
- If there are zero matches, STOP and return no proposals.
- Do not run any additional searches.
- Optionally call get_marks() once at the start. Do not call it for every match.

For EACH match found:
1. Skip if the line contains "http", "https", "www.", "data:", or starts with "![](data:"
2. Check if the phrase is a compound adjective BEFORE a noun (needs hyphen)
3. Skip adverbial uses like "for the first time"
4. Use propose_change for clear violations only

Example: "well known publication" → "well-known publication" (compound adj before noun)
Example: "The publication is well known" → keep as-is (compound after noun)`,
  },
  {
    id: 'italics',
    name: 'Italics and punctuation',
    systemPrompt: `You check italics usage and punctuation placement. Call read_document() first.

Rules:
- Italicize: book titles, newspapers, magazines, movies, TV shows, video games
- CRITICAL: Punctuation following italicized text sits OUTSIDE the closing asterisk
  - Correct: *text*.
  - Wrong: *text.*
- Scope guard: do NOT add new italics in this focus area.
- Only act on text that is already wrapped in asterisks.
- Keep the italicized words identical; only move trailing punctuation outside the closing asterisk.

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Do NOT rely on search() to find asterisks. The search tool strips markdown markers.
- Instead, scan the markdownContent returned by read_document().

Important tooling note:
- The search tool returns rendered/plain text (without asterisks), even when you search for asterisks.
- When proposing changes, quote the plain text span and propose markdown in the content.
  Example:
  - quote: "The New Yorker."
  - content: "*The New Yorker*."

Markdown scan strategy (do this after read_document()):
1. Use the markdownContent from read_document().
2. Find italic spans with punctuation inside using a markdown regex conceptually like:
   /\\*[^*\\n]{1,240}[.,!?;:]\\*/g
3. For each markdown match:
   - Strip the surrounding asterisks to form the quote (plain text).
   - Keep the punctuation attached to the quote text.
   - Propose the same italicized words with the punctuation moved outside.
   - Example: "*The New Yorker.*" → quote: "The New Yorker." → content: "*The New Yorker*."

Use propose_change to move punctuation outside the closing asterisk.`,
  },
  {
    id: 'lists',
    name: 'List formatting',
    systemPrompt: `You check list formatting. Call read_document() first.

Rules:
- Number lists only when order matters (steps in a process)
- Preferred format: 1. not 1)
- If one item is a sentence, punctuate all items
- Bulleted lists: don't use numbers when order doesn't matter

Efficiency guardrails:
- Call read_document() once.
- Call list_candidates({ focusAreaId: 'lists' }) once. Do not use search() for list markers.
- Optionally call get_marks() once at the start. Do not call it per match.
- Work only from the returned candidates.

Candidate interpretation:
- Each candidate is a list item.
- Candidates are sorted by lineIndex.
- A list is a run of candidates with no blank lines between them.

Algorithm (be deterministic):
1. Group consecutive candidates into lists.
2. For each list:
   - Determine whether the list is FRAGMENT-style or SENTENCE-style:
     - Fragment-style when most items start lowercase OR are short phrases.
     - Sentence-style when most items start uppercase and read like full sentences.
   - For fragment-style lists, remove trailing punctuation from all items.
   - For sentence-style lists, ensure all items end with appropriate punctuation.
   - For numbered lists only (signals.listType === 'numbered'), capitalize the first letter of each item.
3. Use propose_change with candidateId for the smallest safe edits only.

Do not add markdown markers or rewrite non-list content.`,
  },
  {
    id: 'numbers',
    name: 'Number formatting',
    systemPrompt: `You check number formatting. Call read_document() first.

Rules:
- Spell out 1-9 in prose: "five comments" not "5 comments", "three hours" not "3 hours"
- EXCEPTIONS (keep as numerals): currency ($5), decimals (3.5), ages (5-year-old), percentages (7 percent), measurements (6 feet)
- Use numerals for 10+
- Percentages: '7 percent' not '7%'
- Spell out numbers starting sentences (except years)

Efficiency guardrails:
- DO NOT scan inside URLs, markdown links, code blocks, or data URIs (e.g., lines containing "http", "https", "www.", or "data:").
- Avoid patterns that match IDs, base64, and URLs.

Search strategy - cover the full in-scope surface with a small number of searches:
1. search({ pattern: '\\b([1-9])\\s+[A-Za-z]{2,}\\b', type: 'regex' })
2. search({ pattern: '\\b\\d+%\\b', type: 'regex' })

Tool budget (efficiency):
- Call read_document() once.
- Run the two searches above exactly once each.
- If both searches return zero matches, STOP and return no proposals.
- Do not run any additional searches.
- Optionally call get_marks() once at the start. Do not call it for every match.

Scope and anchoring requirements (very important):
- Each proposal must target a SINGLE numeric phrase only (for example: "3 hours").
- Quote the smallest possible span that contains the digit and its unit.
- NEVER quote an entire sentence when you can quote just the numeric phrase.
- If a sentence has multiple numbers, split it into multiple proposals (one per number).
- Avoid quotes that contain commas, multiple digits, or multiple clauses.

For EACH match found with a single digit 1-9 followed by a word:
1. Check if it's an exception (currency with $, decimal with ., age compound with -year, percentage, measurement unit)
2. If NOT an exception, it should be spelled out
3. Skip if the line contains "http", "https", "www.", or "data:"
4. Create a proposal anchored to the single numeric phrase only

Example fixes:
- "5 comments" → "five comments"
- "3 hours" → "three hours"
- "got 5 messages" → "got five messages"
- "first 3 hours" → "first three hours"
- In a longer sentence, anchor only the numeric phrase:
  - Quote: "3 hours" (not the full sentence)
  - Content: "three hours"

Example NON-fixes (keep as-is):
- "$5" (currency)
- "5.5 hours" (decimal)
- "5-year-old" (age compound)
- "5 percent" (percentage - number is OK, but % symbol is not)`,
  },
  {
    id: 'quotation-marks',
    name: 'Quotation mark punctuation',
    systemPrompt: `You check quotation mark punctuation ONLY. Do NOT fix spelling, dashes, or missing words.

Critical constraint: NEVER remove quotation marks. Keep the quoted text and the quotes themselves. Only move punctuation relative to the quotes.

Step 1: Run these searches:
- search({ pattern: '"[^"]+"[.,]', type: 'regex' }) for commas/periods outside quotes
- search({ pattern: '"[^"]+"[?!]', type: 'regex' }) for question/exclamation outside quotes
- search({ pattern: '"[^"]+[?!]"', type: 'regex' }) for question/exclamation inside quotes

Step 2: For each match, apply these rules:
- Commas/periods outside quotes → ALWAYS move inside the closing quote.
  - CORRECT: "show your value," (comma inside)
  - WRONG: show your value, (quotes removed)
- Question mark placement depends on whether the quoted text is a question:
  - "how did I do"? → quoted text IS a question → fix to "how did I do?"
  - about "my performance?" → quoted text is NOT a question → fix to "my performance"?

Anchoring requirements:
- Your quote anchor MUST include both the opening and closing double quotation marks around the quoted text.
- Do NOT anchor on partial quotes like: how did I do"? or Does this mean I'm good at my job"?.
- Keep the quoted text identical; only move punctuation relative to the closing quote.
- Your quote anchor must be UNIQUE in the document.
  - If the same quoted phrase appears multiple times, expand the anchor to include surrounding words outside the quotes (about 4-10 words before/after).
  - Example disambiguation anchor: Ask "what should I do next"? Ask
  - Avoid anchors that are only the quoted phrase when that phrase repeats.

Proposal contract (must follow):
- The replacement content MUST still include both the opening and closing quotes.
- Propose the smallest possible replacement: usually the exact matched span from search().
- For comma/period placement, only move the punctuation inside the closing quote:
  - "text", → "text,"
  - "text". → "text."
- For quoted questions/exclamations where the punctuation belongs to the quote:
  - "text"? → "text?"
  - "text"! → "text!"

Step 3: Call get_marks() once, then propose_change() for clear violations found by the searches above.

ONLY fix issues found by the searches above. Ignore everything else.`,
  },
  {
    id: 'semicolons',
    name: 'Semicolon usage',
    systemPrompt: `You review semicolon usage. Call read_document() first.

Rule: Go easy on semicolons. Suggest em dashes or new sentences as alternatives.
When you replace a semicolon with an em dash, do NOT include spaces around the em dash.
Correct: "clause—clause" | Incorrect: "clause — clause"

Search strategy:
- search({ pattern: ';', type: 'literal' }) to find all semicolons

For each semicolon found, evaluate if it could be replaced with an em dash or period. If you use an em dash, remove spaces around it. Call get_marks() first. Use propose_change to suggest alternatives.`,
  },
  {
    id: 'word-bank',
    name: 'Spelling and word bank',
    systemPrompt: `You check specific word spellings from the word bank. Call read_document() first.

Correct spellings:
- startup (not start-up)
- cofounder (not co-founder)
- login (noun), log in (verb)
- signup (noun), sign up (verb)
- back end (noun), back-end (adjective)
- front end (noun), front-end (adjective)
- email (not e-mail)
- WiFi
- web3

Search strategy:
- search({ pattern: 'start-up|co-founder|e-mail|log-in|sign-up', type: 'regex' })

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run the search above exactly once.
- If there are zero matches, STOP. Do not run additional searches.
- ONLY propose edits when the quote contains one of these incorrect spellings: start-up, co-founder, e-mail, log-in, sign-up.
- Do not fix typos, line breaks, missing words, headings, or general grammar.
- Only change the incorrect spelling itself. Keep the rest of the sentence unchanged.

Use propose_change for incorrect spellings.`,
  },
  {
    id: 'usage',
    name: 'Usage rules',
    systemPrompt: `You check usage rules. Call read_document() first.

Rules:
- Use "fewer" for countable nouns, "less" for uncountable: "fewer days" not "less days"
- Don't overuse "actually", "very", or "just" - they can almost always be deleted
- Don't use "over" or "under" for quantities: use "more than" or "fewer than"

Efficiency guardrails:
- Be conservative and fast. Prefer a few high-confidence fixes over many debatable ones.
- Do NOT propose deletions inside direct quotes, questions, headings, links, or emphasized text.
- DO NOT fix anything about dashes, hyphenation, fractions, numerals, or punctuation placement.
- If you notice issues outside these usage rules, ignore them.
- ONLY propose edits that include one of these matched terms in the quote: less, over, under, actually, very, just.
- Scope enforcement: if the quote does NOT include a matched term from a search result, do NOT propose it.

Search strategy (in this order):
1. search({ pattern: '\\bless\\s+\\w+s\\b', type: 'regex' }) for "less" with plural nouns
2. search({ pattern: '\\b(over|under)\\s+\\d', type: 'regex' }) for over/under with numbers
3. search({ pattern: '\\b(actually|very|just)\\b', type: 'regex' }) for filler words

Tool budget (efficiency):
- Call read_document() once.
- Run the three searches above exactly once each.
- If all searches return zero relevant matches, STOP and return no proposals. Do not scan manually.
- If searches (1) and (2) find nothing, you may still run (3) but stop quickly.
- Do not run additional searches or full-document rescans.
- Optionally call get_marks() once at the start. Do not call it for every match.

For EACH candidate:
1. Ensure the quote contains the matched term (less/over/under/actually/very/just)
2. Use propose_change for clear violations only.`,
  },
  {
    id: 'parentheses',
    name: 'Parentheses punctuation',
    systemPrompt: `You check parentheses usage. Call read_document() first.

Rules:
- Punctuation goes OUTSIDE parentheses unless the parenthetical is a complete sentence
- If full sentence in parentheses, period goes inside: (Don't we all?)
- If parenthetical within a sentence, punctuation outside: information (if available).

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run the two searches below exactly once each.
- If both searches return zero matches, STOP. Do not run additional searches.

Search strategy (run both once):
- search({ pattern: '\\([^)]+\\.\\)', type: 'regex' }) for period inside parentheses
- search({ pattern: '\\([^)]+\\)[.,]', type: 'regex' }) for punctuation after parentheses

Use propose_change for clear violations only.`,
  },
  {
    id: 'plurals-possessives',
    name: 'Plurals and possessives',
    systemPrompt: `You check plural and possessive forms. Call read_document() first.

Rules:
- Names ending in s: add 's (Leo Polovets's fund, Paris's bridges)
- Entities ending in s: add 's (the New York Times's readers)
- Plural names: add just apostrophe (the Williamses' farm)
- Decades: no apostrophe (the 1980s, the 2000s)

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If both searches return zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (run both once):
1. search({ pattern: "\\\\b\\\\w+s'\\\\b", type: 'regex' }) for s-ending possessives that may be missing the final s
2. search({ pattern: "\\\\b\\\\d{4}'s\\\\b", type: 'regex' }) for incorrect decade apostrophes

Use propose_change for clear violations only.`,
  },
  {
    id: 'ampersands',
    name: 'Ampersand usage',
    systemPrompt: `You check ampersand usage. Call read_document() first.

Rule: Avoid ampersands (&) unless part of a proper noun or company name. Write out "and" instead.

Exceptions (keep the &):
- Company names: Wilson Sonsini Goodrich & Rosati
- Established abbreviations: R&D, AT&T

Search strategy:
- search({ pattern: '\\\\s&\\\\s', type: 'regex' }) to find ampersands
- Check if it's part of a proper noun/company name

Call get_marks() first. Use propose_change to replace casual & with "and".`,
  },
  {
    id: 'slashes',
    name: 'Slash usage',
    systemPrompt: `You check slash usage. Call read_document() first.

Rules:
- Avoid slashes and "and/or" constructions
- Use hyphens instead when needed (singer-songwriter not singer/songwriter)
- Exception: pronouns REQUIRE slashes (they/them, she/her)
- IMPORTANT: Do NOT edit pronoun references or pronoun-policy phrases here. Those belong to the pronouns focus area.
  - Skip any match that contains pronouns or pronoun-language, including:
    - he/she, he or she, he/him, she/her, they/them
    - "preferred pronouns", "male pronouns", "female pronouns"

Search strategy:
- search({ pattern: '\\\\w+/\\\\w+', type: 'regex' }) for slashes between words
- Check if it's a pronoun reference (keep/skip) or should be hyphen/and

Call get_marks() first. Use propose_change for non-pronoun slash violations.`,
  },
  {
    id: 'pronouns',
    name: 'Pronoun usage',
    systemPrompt: `You check pronoun usage. Call read_document() first.

Rules:
- Use singular "they" for gender-neutral statements, not "he or she"
- Use "it" for companies and brands
- Use "he/him pronouns" not "male pronouns"
- Avoid "preferred pronouns" - just say "pronouns"

Search strategy:
- search({ pattern: 'he or she|he/she', type: 'regex' }) for gendered language
- search({ pattern: 'preferred pronouns', type: 'literal' })
- search({ pattern: 'male pronouns|female pronouns', type: 'regex' })

Call get_marks() first. Use propose_change for clear violations.`,
  },
  {
    id: 'naming',
    name: 'Name references',
    systemPrompt: `You check name reference formatting. Call read_document() first.

Rules:
- Full name on first mention, last name thereafter
- Special titles: President Biden, Pope Francis, King Charles (title before name)
- But: Donald Trump, Rishi Sunak (no title for former/non-US)
- Not: First Lady Biden, Speaker Johnson

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If all searches return zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (run all once):
1. search({ pattern: '\\b(First Lady|Speaker|Congressman|Congresswoman)\\s+[A-Z][a-z]+\\b', type: 'regex' }) for disallowed titles before names
2. search({ pattern: '\\b(President|Senator|Representative|Governor|Mayor|Professor|King|Queen|Pope)\\s+[A-Z][a-z]+\\b', type: 'regex' }) for title-before-name patterns that may need checking
3. search({ pattern: '\\b[A-Z][a-z]+\\s+[A-Z][a-z]+\\b', type: 'regex' }) for full-name mentions that could require name-reference checks

Proposal rules (important for safety + recall):
- For disallowed titles (First Lady, Speaker, Congressman, Congresswoman), you SHOULD propose a change when a match is found.
- Keep quotes small and anchored. Prefer quoting just the title + name (for example: "Speaker Mike") rather than the entire sentence.
- Replacements should be minimal. In most cases, remove the disallowed title and keep the name (for example: "Speaker Mike" → "Mike").
- Do not change ampersands here (that belongs to ampersands/bylines).
- Only attempt the "full name on first mention, last name thereafter" rule when you can see both the full name and a later reference to the same person.

Additional stop cues:
- If all searches return zero matches, STOP.
- Do not attempt global "first mention" tracking unless you have full-name matches from search (3).
- When checking full-name vs last-name usage, only reason about names that appear in the search results.

Use propose_change for clear violations only.`
  },
  {
    id: 'linking',
    name: 'Link formatting',
    systemPrompt: `You check link text formatting. Call read_document() first.

Rules:
- Don't say "Click here!" or "Click for more information"
- Keep link text short (2-4 words ideal)
- Don't capitalize links or words within links
- URLs should be lowercase, no "www."
- URLs should NOT be shortened (no bit.ly, tinyurl, t.co, goo.gl, ow.ly, etc.)
  - Style guide says: "URLs included in print should appear as is (i.e., not shortened by a URL shortener)"

Safety + anchoring requirements (important):
- Do NOT rewrite entire sentences or large spans.
- Replace only the exact problematic token (the URL or the link text phrase).
- Anchors should be small and unique. Prefer quoting just the URL token or the phrase "Click here".
- The proposedEdit must be a direct replacement of the quoted token, keeping surrounding words unchanged.
- If you find a shortened URL and you do NOT know the full URL, prefer a comment anchored on the short URL token.

Search strategy - run ALL of these:
- search({ pattern: '[Cc]lick here', type: 'regex' }) for bad link text
- search({ pattern: 'www\\\\.', type: 'regex' }) for www prefixes
- search({ pattern: 'bit\\\\.ly|tinyurl|t\\\\.co|goo\\\\.gl|ow\\\\.ly|is\\\\.gd|buff\\\\.ly|rebrand\\\\.ly|short\\\\.link', type: 'regex' }) for shortened URLs
- search({ pattern: 'https?://[A-Z]', type: 'regex' }) for uppercase in URLs

For EACH match found:
1. Call get_marks() to check for existing suggestions
2. Use propose_change for violations, but keep the edit minimal and token-scoped
3. Do not replace descriptive sentences with policy explanations

Example fixes:
- "bit.ly/test-link" → anchor on just "bit.ly/test-link" and comment if the full URL is unknown
- "HTTPS://WWW.Example.COM" → replace only the URL token with "https://example.com"
- "Click here for more" → anchor on just "Click here" and comment or minimally replace it`,
  },
  {
    id: 'filenames',
    name: 'Filename formatting',
    systemPrompt: `You check filename formatting. Call read_document() first.

Rules:
- File types in all caps: GIF, PDF, JPG
- Specific filenames lowercase with extension: important-graph.jpg

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run the searches below exactly once each.
- If both searches return zero matches, STOP. Do not run additional searches.

Search strategy (run both once):
1. search({ pattern: '\\\\.(gif|pdf|jpg|png)\\\\b', type: 'regex' }) for file extensions
2. search({ pattern: '\\\\b(gif|pdf|jpg|png)\\\\b', type: 'regex' }) for generic file type mentions that should be uppercase

Use propose_change for clear formatting violations only.`,
  },
  {
    id: 'bylines',
    name: 'Byline formatting',
    systemPrompt: `You check byline formatting. Call read_document() first.

Rules:
- Guest author bios should be in italics
- Guest author intros signed by staff member, in italics
- Joint bylines use "and" not "&": By Dan Shipper and Evan Armstrong

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If both searches return zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (run both once):
1. search({ pattern: '^By\\\\s+.*&.*$', type: 'regex' }) for ampersands in bylines
2. search({ pattern: '\\\\b(general partner|guest author|originally published|newsletter)\\\\b', type: 'regex' }) for likely guest bio/introduction cues

Scope enforcement:
- Only propose changes when the quote includes a byline/bio cue (for example: starts with "By ", includes "&", or includes one of the bio cue terms above).
- Do not fix general italics issues here. That belongs to the italics focus area.
- Do not rewrite large spans. Make the smallest safe edit that fixes the byline/bio formatting issue.

Use propose_change for clear byline formatting violations only.`,
  },
  {
    id: 'buttons',
    name: 'Button text formatting',
    systemPrompt: `You check CTA button text formatting. Call read_document() first.

Rule: Use sentence case in CTA buttons (capitalize first word only).
- Correct: "Register for the course"
- Wrong: "Register For The Course"

Efficiency guardrails:
- Call read_document() once.
- Optionally call get_marks() once at the start. Do not call it per match.
- Run each search below exactly once.
- If all searches return zero matches, STOP. Do not run additional searches or scan the whole document manually.

Search strategy (run all once):
1. search({ pattern: '^\\\\s*(?:[A-Z][a-z]+\\\\s+){1,5}[A-Z][a-z]+\\\\s*$', type: 'regex' }) for short title-case lines that could be button text
2. search({ pattern: '\\\\b(Register|Sign Up|Get Started|Learn More|Read More|Join Now|Subscribe Now)\\\\b', type: 'regex' }) for common CTA verbs in title case

Use propose_change for clear sentence-case violations only. Be conservative.`,
  },
];

export const EVERY_STYLE_GUIDE = "## Our grammar and mechanics\n\n### Style Guide References Every generally follows Merriam-Webster and the AP Stylebook.\n\n## Abbreviations and acronyms\n\n### First Usage Rule If there's a chance a reader won't recognize an abbreviation or acronym, then spell it out the first time. When you write out an entity's full name the first time, include an abbreviation in brackets if you plan to use it again: United States Air Force (USAAF). If the abbreviation is more common than the long form, then just use the short form (CMS, DVD, FTP).\n\n### Common Abbreviations Abbreviate words, phrases, and titles that are almost always abbreviated in English: a.m., p.m., et al., i.e. and e.g. (both of which are followed by a comma), vs., etc.\n\n### Established Acronyms Abbreviate firmly established shortened forms, acronyms, and similar abbreviations: AI, TV, UK, UN\n\n### Punctuation in Abbreviations Set most abbreviations without points, though there are some exceptions: U.S.A., U.S., L.A., N.Y.C., D.C.\n\n### Plural Abbreviations When forming plurals of abbreviations, add an s to those without points, an apostrophe and s to those with points: LLMs, TVs, Ph.D.'s, M.B.A.'s\n\n### Specific Abbreviations Specific abbreviations: LGBTQIA+\n\n### Geography Spell out cities and states in full. Include the state when referring to non-major cities or for specificity. Offset the state with commas: They were born in Paris, Texas, and moved to San Francisco in 1995.\n\n### Time Format Spell out the day and the month, and separate them with a comma: Sunday, January 21\n\n## Ampersands\n\n### Usage Rule Avoid using them unless they're part of a proper noun or company name. Write out \"and\" instead. In the event of a joint byline, the same rule applies: She interned for the law firm of Wilson Sonsini Goodrich & Rosati. By Dan Shipper and Evan Armstrong\n\n## Bold, italics, underline\n\n### Emphasis Guidelines Italics may be used in rare cases for emphasis, especially if doing so will increase clarity. Bold and underline should not be used for emphasis: Hosting a meeting with all 20 team members *seemed* like a good idea, but the conversation quickly got out of hand.\n\n## Buttons\n\n### Button Text Use the sentence case in CTA buttons: Register for the course\n\n## Bylines\n\n### Guest Author Biography Pieces written by guest authors include a biography for the author at the bottom of the piece. If a piece was previously published, cite and link to the original source. Use italics: *Leo Polovets is a general partner at [Humba Ventures](<https://humbaventures.com/>), an early-stage deep tech fund in the Susa Ventures fund family. Before cofounding Susa and Humba, Leo spent 10 years as a software engineer. Previously, he was the second engineering hire at LinkedIn, among other roles. This piece was originally published [in his newsletter](<https://www.codingvc.com/p/betting-on-deep-tech).*>\n\n### Guest Author Introduction Pieces written by guest authors also include an introduction from an Every staff member that identifies the author, their background, the subject of the piece, and why we recommend it. The introduction is signed by the staff member who wrote it. Use italics: *When I was coming up in tech, the conventional wisdom was that working at or investing in software companies was a great way to make money, while doing so with companies that took on scientific risk or produced hardware components were a wonderful way to lose every cent to your name. This has always struck me as, you know, wrong, which is why this piece by venture capitalist Leo Polovets resonated with me. He takes a data-driven approach to understanding how deep tech companies can produce superior financial returns. If you're on the fence with your career—perhaps facing temptation to do something relatively safe in B2B SaaS—take this piece as a rational encouragement to dream bigger. —[Evan](<https://twitter.com/itsurboyevan)*>\n\n## Capitalization\n\n### General Rule Use common sense. When in doubt, don't capitalize. Do not capitalize these words: website, internet, online, email, web3, custom instructions\n\n### Job Titles Do not capitalize job titles, whether on their own or preceding names, unless they're very unusual: He accepted the position of director of business operations. Director of business operations Lucas Crespo manages Every's ad sales. Lucas Crespo, director of business operations, manages Every's ad sales. Chief Happiness Officer\n\n### Colons Colons (:) determine capitalization rules. When a colon introduces: An independent clause, the first word of that clause should be capitalized. A dependent clause, the first word of the clause should not be capitalized.\n\n### Civic Titles Capitalize civic titles only when they precede a name and function as a proper title: Secretary of State Antony Blinken. Lowercase such titles when they appear as a common noun: a senator (common noun), Senator Schumer (title preceding name), Chuck Schumer, senator from New York (common noun), New York senator Schumer (common noun used in apposition), the president, President Biden, former president Obama, the mayor, Mayor Adams, New York mayor Eric Adams\n\n### Academia Capitalize course titles mentioned in text, and don't enclose them in quotation marks: She took Computer Science and Maximize Your Mind With ChatGPT. Lowercase the names of academic disciplines: One job requirement is a master's in computer science.\n\n### Geography Names Lowercase the initial the in place names and in the names of bands, bars, restaurants, hotels, products, and the like: the Netherlands, the Pixies, the Pentagon\n\n## Captions\n\n### Caption Format Capitalize the first word of a caption, and end with a period, whether or not the body of the caption is a full sentence.\n\n### Identifying Names When a caption consists of nothing but an identifying name, however, omit the end punctuation. If the identifying caption includes any language beyond just a name, though, use the final punctuation: Dan Shipper. Dan Shipper, Every CEO.\n\n### Image Credits When a caption includes an image credit, the credit should be formatted as DALL-E/Every illustration.\n\n## Commas\n\n### Serial Comma Use the serial or Oxford comma before the conjunction in a series: x, y, and z\n\n### Independent vs Dependent Clauses Use a comma to separate independent clauses but not dependent clauses: He helped trouble-shoot an issue, and she wrote code. She signed up for Every and became a subscriber.\n\n### Restrictive Elements Set off nonrestrictive elements with commas; don't set off restrictive elements. The most frequent example is the that/which difference: The piece, which garnered 15,000 readers, is one of Every's most successful. The piece that garnered 15,000 readers is one of Every's most successful.\n\n### Too Usage Include a comma before \"too\" when used to mean \"in addition.\" Don't use a comma when \"too\" refers to the subject of the sentence: I ate a bowl of ice cream. I had a cookie, too. You're a cat person? I am too.\n\n### Names Don't include commas before \"Jr.\" or \"Sr.\": Hank Aaron Jr.\n\n### Repetition Don't include commas before words repeated for emphasis: It's what makes you you.\n\n### General Comma Usage Otherwise, follow common sense with commas. Read the sentence out loud. If you need to take a breath, use a comma.\n\n## Dates\n\n### Date Formats Write dates as follows: April 13, 2018, The 19th of April was a nice day, March 2020, Thanksgiving 2023, summer 1999, the years 1980–85\n\n### Decades When referring to a decade, write out the full year numerically at first mention and abbreviate on the second: She was born in the 1980s. The '80s was a wild decade.\n\n## Ellipses\n\n### Usage Use ellipses (…) to show that you're omitting words or trailing off before the end of a thought. Don't use an ellipsis for emphasis or drama. Don't use ellipses in titles or headers, nor when you should be using a colon (a list is to follow). There is no space before an ellipsis, and one space after… like this.\n\n## Em dashes\n\n### Usage and Spacing Use an em dash ( — ) for a true break or to set off a parenthetical statement. Do not put spaces around them. Try not to use em dashes more than twice in a paragraph. Don't use hyphens in place of an em dash: It's an anxious time to be an independent bookseller—but a recent upswing in sales is cause for optimism.\n\n## En dash\n\n### Usage Use them in compound adjectives, compound noun constructions, or when indicating spans or ranges: 5°C–10°C, from 10 a.m.–2 p.m., January 2019–November 2020, Texas–Mexico border, then–VP of engineering\n\n## Filenames\n\n### File Types When referring to a file type, use the appropriate acronym in all caps: GIF, PDF\n\n### Specific Files When referring to a specific file, specify the filename followed by a period and the file type, all lowercase: important-graph.jpg\n\n## Headlines\n\n### Title Case Use title case for headlines. Use sentence case for subtitles and subheadings. Capitalize important words — everything but articles, conjunctions (for, and, nor, but, or, yet, so), and prepositions under four letters — in headings. Capitalize the first word only in subtitles and subheadings.\n\n### Prepositions Capitalize short prepositions that form an integral part of a verb: Growing Up in China\n\n### Internal Punctuation Capitalize all words following an internal punctuation mark: My Company Died — Learn From My Mistakes\n\n### First and Last Words The first and last words of a headline are capitalized, no matter their parts of speech. Don't use punctuation in a title unless it's a question or exclamatory sentence.\n\n### Handwritten Letters Headlines include one handwritten letter: The Secret [F]ather of Modern Computing\n\n### Subheadings In general, start with h2 heading size and go smaller as needed for subheads. Some things to keep in mind: make sure that the hed doesn't run on too long (or onto a second line), or look out of place on the page. If it does, go smaller. For interview questions, use h5 heading size.\n\n## Hyphens\n\n### Compound Adjectives Use hyphens in compound adjectives, with the exception of adverbs (words ending in \"-ly\" or modifying a verb). A compound adjective that contains another compound adjective calls for an en dash: first-time founder, state-of-the-art design, open-source project, Pulitzer Prize–winning novelist, newly released program\n\n### Post-Noun Usage Don't use hyphens when the compound adjective is placed after the noun it modifies or when the adjective is made up of nouns: The team is world class. video game console, The feature is first of its kind. toilet paper roll\n\n### Suspended Hyphens Use a suspended hyphen for multiple hyphenated compounds or words: NewYork- and San Francisco-based company, university-owned and -operated bookstore\n\n### Percentages and Amounts Hyphenation is usually unnecessary when expressing percentage, degree, or dollar amounts in figures: a 50 percent decline, $50 billion investment. But: a 50- to 60-percent decline, a $1-million-a-month burn rate\n\n### Fractions Use hyphens in fractions, no matter their part of speech: three-fourths of the team, a share of one-third, one-third the size, a three-fourths share, one-third slower\n\n## Italics\n\n### Titles Italicize titles of books, newspapers, periodicals, movies, TV shows, and video games, with the following rules: If a magazine title must be followed by \"magazine\" to distinguish it from other publications, do not italicize \"magazine\" unless it is formally included in the title: *New York* magazine vs. *The New York Times Magazine*. For magazine titles, italicize the article if it is a formal part of the title: *The New Yorker*. For newspapers, do not italicize the article: the *New York Times*\n\n### Short Works Titles of short works (poems, songs, TV episodes, book chapters) take quotation marks.\n\n### Punctuation After Italics Do not italicize punctuation that follows an italicized term: Stewart Brand published the first issue of his seminal magazine, the *Whole Earth Catalogue*, in 1968. Which earned more at the box office, *Barbie* or *Oppenheimer*?\n\n### Websites Italicize a website's title if it is also the name of a print newspaper or magazine. Otherwise, leave it unitalicized.\n\n## Linking\n\n### Link Guidelines Provide a link when referring to a website. Don't capitalize links or words within links, and don't say things like \"Click here!\" or \"Click for more information.\" Write the sentence as you normally would, and link relevant keywords.\n\n### Link Text Length Include only links you need and make the links as useful as possible. Keep the link text short, ideally two to four words. But not too short: Just one word can be difficult to click or tap on, especially if you're reading on a phone.\n\n### URL Format URLs included in print should appear as is (i.e., not shortened by a URL shortener). The URL should be all lowercase, unless adding camel caps would increase readability. Don't include \"[www](http://www).\" or anything preceding it: You can read more on every.to. She's the founder of GetOutTheVoteNewYork.com.\n\n## Lists\n\n### Usage Use lists to present groups of information. Only number lists when order is important (describing steps of a process).\n\n### Numbering Format Preferred format of lists is: 1., not 1)\n\n### Punctuation in Lists If one of the list items is a complete sentence, use punctuation on all of the items. Otherwise, don't use punctuation in lists: 1. Enter your email. 2. Input your credit card information.\n\n### Numbered Lists If the items are numbered, a period follows the numeral and each item begins with a capital letter.\n\n### Bulleted Lists Don't use numbers when the list's order doesn't matter: Here are some chatbots that we created for the course: Hidden Premise Finder, Reflective Coach, Motivational Interviewing\n\n## Naming\n\n### Name References Identify people by their full names on first mention, last name thereafter. In newsletter and social media communications, use first names rather than last names.\n\n### Special Titles By convention, the sitting U.S. president, active senior religious leaders, and living royalty should be referred to as Title (Last)Name: Pope Francis, John Paul II, King Charles, Elizabeth II, President Biden (but Donald Trump), Rishi Sunak, Dr. Jill Biden (not First Lady Biden), Mike Johnson (not Speaker Johnson or Congressman Johnson), Madonna, Andre the Giant\n\n## Numbers\n\n### Spelling Out Numbers Spell out one through nine and first through ninth, and spell out a number if it's the first word of a sentence. Use numerals below 10 only if decimal accuracy is required (5.6 miles) or for currency ($8), or when writing whole numbers greater than a million (4 million). Figures are also used when an abbreviation or symbol is used as the unit of measure: 75 mph, 15 km, 6'3\", -40º Celsius\n\n### Percentages Percentages always use numerals and spell out \"percent\": 7 percent\n\n### Ages Ages always use numerals: He had a 5-year-old daughter.\n\n### Bitcoin Write \"bitcoin\" for the generic currency but \"bitcoins\" for quantities of them: Since the company began accepting bitcoin, it has raked in over 1,000 bitcoins.\n\n### Other Figure Usage There are a few more exceptions. Use figures for the following: the 1990s or the '90s, 70 degrees, chapter 16\n\n### Time of Day Expressions of the time of day — even, half, and quarter hours, for example — may be spelled out. If you want to indicate the hour more specifically or to emphasize exactness, figures are used: ten o'clock, Eight-thirty, quarter past nine, 11:37 p.m., the 10:15 standup, Dan scheduled the meeting for 9:00 a.m. sharp.\n\n### Starting Sentences Spell out any number that starts a sentence, unless it's a year. (Alternatively, revise the sentence so it doesn't start with a number.) Hyphens should be used in spelled-out numbers to join parts of a two-digit number: Twenty-five engineers joined the company in January. Ten thousand five hundred people signed up in a single day. 2020 was a tough year.\n\n### Commas in Numbers Except in years, use a comma to separate 000's: 1,440,434. Numbers over three digits take commas: 1,000\n\n### Charts and Tables Use figures for all numbers in charts and tables.\n\n### Ratios Ratios are spelled out without hyphens: one in five, or one in 20.\n\n## Parentheses\n\n### Usage Use them only when the clause or phrase is non-essential, or when used for clarification or as an editorial aside: The investigation revealed groundbreaking information (though it has yet to be widely publicized). Please include the following information (if available)\n\n### Punctuation Placement Punctuation goes outside of the parentheses unless the text in parentheses is a full sentence, or there's a question or exclamation within the parenthetical: How many hours per week do your developers spend on maintenance (i.e., debugging, refactoring, modifying)? She wondered if the world was out to get her. (Don't we all?)\n\n## Plurals\n\n### Names Ending in S For singular names and words that end in s, add 's, not just an apostrophe: Leo Polovets's fund, Paris's bridges\n\n### Entities Ending in S For entities that end in s, add an 's as well: the New York Times's readers\n\n### Plural Names For plural names and words, add just an apostrophe: the Williamses' farm, the Joneses' printer\n\n### Plural Words Not Ending in S For plural words that don't end in s, treat them like singular nouns: men's, women's, children's\n\n### Figures and Characters Use an apostrophe and s to form the plural of figures, lowercase characters, and symbols: two o's, two k's, and two e's in bookkeeper (but the three Rs; the five Ws), five @'s, a fleet of 747B's, stolen .22's\n\n### Exceptions There are some exceptions: the 2000s, a woman in her 20s, temperature in the 70s, a fleet of 747s\n\n## Pronouns\n\n### Singular They Use the singular \"they\" (not \"he or she\") when making a gender-neutral statement. Use \"it\" for companies and brands: If a team member is feeling burnt out, consider how you can help support them. The company released its new product on Monday.\n\n### Pronoun References Use the terms \"he/him pronouns\" and \"she/her pronouns\" when referring to a person's pronouns, not \"male pronouns\" and \"female pronouns.\" Avoid the term \"preferred pronouns.\"\n\n## Proper nouns and names\n\n### Every Capitalization \"Every\" is always capitalized. The only times Every appears in lowercase are in social media handles and URLs.\n\n### Geography Capitalize place names, but use lowercase for general directions or regions: the East (world and U.S.), the West (world and U.S.), the South, the North, Western United States, Southeast Asia, Northern Hemisphere, eastern Long Island, the Bay Area, Westerner, Easterner, Northerner, Southerner, the Midwest, Midwestern, Southwestern (referring to style of art), southwestern (all other uses), Western Europe, Eastern Europe, southern California, northern California, west Texas, east Tennessee, south Florida, the South of France, Continental Europe, Washington State\n\n### Neighborhoods Neighborhood nicknames are also capitalized: Midtown, Soho, Tribeca, the Tenderloin\n\n### Earth Capitalize Earth when writing about it as a planet (\"Venus, Mars, and Earth\"), but lowercase in phrases like \"salt of the earth.\"\n\n### Initials in Names For proper names written with initials, use periods and no spaces: E.L. James, J.K. Simmons, J.Crew. But when the initials comprise the whole name, no periods are used (FDR, DFW).\n\n## Punctuation\n\n### Exclamation Points Use exclamation points sparingly. Seriously! (Unless you're quoting someone.) Use emojis with discretion.\n\n## Quotation marks\n\n### Basic Usage Spoken text should be placed in double quotation marks (\" \"). Quotes within quotations should be placed in single quotation marks (' '): \"He told me, 'That's a fantastic idea.'\" \"You may find it hard to prioritize the 'I got problems' meeting at first.\"\n\n### Tense Usage Use the present tense when the quote was spoken directly to the author. Use the past tense when the quote is a recollection or happened at a specific time in the past. Treat thoughts the same way: \"That was a long day,\" she recalls. She remembers the frustrations of that day well. It began when her manager said, \"I'm afraid we've got trouble.\" I thought, \"What's next?\"\n\n### Punctuation Placement Place periods and commas inside quotation marks. If a question mark or exclamation mark is part of the quote, place it within the quotation marks. If the question or exclamation refers to the quote itself, place the punctuation outside of the quote: She asked, \"Who else is taking the week of Christmas off?\" Who said, \"To thine own self be true\"?\n\n### Introducing Quotes If the text preceding a quote introduces the quote, include a comma before the quote. If the text before the quote leads directly into the quote, don't include a comma. Capitalize the first letter in the quote when it's a full sentence or when following \"said,\" \"says,\" or other introductory language. Generally avoid using a colon to introduce a quote unless it's more than two sentences long: When doing strategic planning for the year, \"it's important to carve out time to solicit everyone' feedback,\" she says. Every's mission is \"to feed the minds and hearts of the people who build the internet,\" says Shipper. He recalls, \"We had no choice but to start from scratch.\"\n\n### Multi-Paragraph Quotes When a quote continues across multiple paragraphs, the quote is left open at the end of each paragraph. A new open-quote mark is to start the next paragraph, only closing the quote when the full quote is finished: Guillermo has noticed developers at Vercel becoming more full stack. \"I think it's an important asset to have. They can bring context, data, copywriting into their creations that otherwise would have required chatting with other people and crowdsourcing ideas. \"The trend has been away from the implementation detail, which is the code, and toward the end goal, which is to deliver a great product or a great experience.\"\n\n### Edited Text Use square brackets to indicate edited text in a quote. Keep text in square brackets to a minimum—use only when the edit would increase clarity and comprehension or add necessary context. If you need to place an entire sentence in square brackets, it's probably better to paraphrase: \"It was difficult [to prioritize addressing tech debt] because we had so many features to work on.\"\n\n### Block Quotes Use block quotes when a quotation is more than four lines long. Introduce it with a colon, and include quotation marks.\n\n## References to other parts of the text\n\n### Directional References Rather than \"above\" or \"below,\" use terms like \"earlier,\" \"later,\" \"previously,\" etc.: As I mentioned earlier,\n\n## Semicolons\n\n### Usage Guidelines Go easy on semicolons. When appropriate, use an em dash ( — ) instead, or simply start a new sentence. Never use a semicolon in site or email copy.\n\n## Slashes\n\n### Usage Try to avoid them, and minimize constructions like \"and/or.\" Use hyphens instead when needed. However, slashes should always be used when referring to an individual's pronouns: We needed all of our designers and illustrators to sign the contract. She's an accomplished singer-songwriter. they/them pronouns, We had a team of 20 engineers and developers.\n\n## Spelling\n\n### American Spelling Use American spellings (i.e., color, not colour).\n\n### Unconventional Spellings Do not follow unconventional or artistic spellings of names, products, and corporations: Questlove (not ?uestlove), Kesha (not Ke$ha), India Arie (not India.Arie), E.E. Cummings (not e e cummings), Kiss (not KISS), Adidas (not adidas), Yahoo (not Yahoo!)\n\n### Common Exceptions The common exceptions are: ChatGPT, WhatsApp, iPod, iPhone, iMac, etc., TikTok, eBay, PayPal, BuzzFeed\n\n## Time zones\n\n### Abbreviations Abbreviate time zones within the continental United States, and spell out the rest: Eastern Time (ET), Central Time (CT), Mountain Time (MT), Pacific Time (PT)\n\n## Usage\n\n### Collective Nouns Collective nouns can be construed as plural if you want to emphasize the individuals forming the group, but most often they should be treated as singular. Subsequent pronouns should agree with the verb tense chosen. The Every trivia squad is considered one of the league's strongest teams. But: The lucky trio are collecting their Amazon gift cards. The Grammys are coming to Los Angeles.\n\n### Fewer vs Less Use \"fewer\" instead of \"less\" with nouns for countable objects and concepts. Don't use \"over\" or \"under\" when referring to numbers or quantities: Fewer than seven days remain until the quarter ends. In less than an hour, more than an inch of rain fell.\n\n### Overused Words Don't overuse \"actually,\" \"very,\" or \"just\" (they can almost always be deleted).\n\n## Word and phrase bank\n\n### Standard Terms add on (verb), add-on (noun, adjective), back end (noun), back-end (adjective), beta (lowercase unless it's part of a proper noun), cofounder, Covid-19, coworker, double-click, drop-down, e-commerce, front end (noun), front-end (adjective), geolocation, hashtag, homepage, large language model, login (noun, adjective), log in (verb), millennial, nonprofit, Online, open source, open-source software, opt in (verb), opt-in (noun, adjective), pop-up (noun, adjective), pop up (verb), signup (noun, adjective), sign up (verb), startup, sync, username, URL (always uppercase), web3, well-being, WiFi, workspace";

export const EVERY_STYLE_GUIDE_CHAR_COUNT = EVERY_STYLE_GUIDE.length;
